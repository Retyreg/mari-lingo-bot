# 🚀 Быстрая настройка Mari Lingo Bot с Groq AI (бесплатно!)

## Почему Groq?

✅ **БЕСПЛАТНО** - 30 запросов в минуту бесплатно  
✅ **БЫСТРО** - в 10-20 раз быстрее чем Claude/ChatGPT  
✅ **КАЧЕСТВЕННО** - использует Llama 3.3 70B модель  
✅ **БЕЗ ЛИМИТОВ** - нет ограничений на количество токенов  

## Шаг 1: Получите Groq API ключ (2 минуты)

1. Откройте: https://console.groq.com/
2. Нажмите **Sign In** → **Continue with Google** (или Email)
3. После входа перейдите в **API Keys** (слева в меню)
4. Нажмите **Create API Key**
5. Дайте название: `Mari Lingo Bot`
6. **СКОПИРУЙТЕ КЛЮЧ** (начинается с `gsk_...`)

⚠️ **Важно:** Ключ показывается только один раз! Сохраните его.

## Шаг 2: Установите библиотеку Groq

```bash
pip install groq
```

## Шаг 3: Обновите .env файл

```bash
nano .env
```

Добавьте/измените строку:

```bash
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
GROQ_API_KEY=gsk_ваш_ключ_от_groq
RAG_DB_PATH=/Users/dmitrijvatutov/Desktop/1AI/Projects/RAG from PDF/files/rag_database
USER_DATA_PATH=./user_data
```

Сохраните (Ctrl+O, Enter, Ctrl+X)

## Шаг 4: Скачайте новый файл бота

Скачайте файл `mari_lingo_bot_groq.py` из списка выше ⬆️

Переместите в папку Bot:

```bash
mv ~/Downloads/mari_lingo_bot_groq.py ~/Desktop/1AI/Projects/Mari\ Lingo/Bot/
```

## Шаг 5: Запустите бота!

```bash
cd ~/Desktop/1AI/Projects/Mari\ Lingo/Bot/
python3 mari_lingo_bot_groq.py
```

Должны увидеть:

```
🚀 Загрузка модели...
📚 Подключение к базе данных...
✅ База загружена (9227 документов)
🚀 Mari Lingo Bot запущен (Groq AI)!
```

## Тестирование

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Задайте вопрос: "Как сказать привет на марийском?"
4. Бот ответит быстро и точно! ⚡

---

## 🆚 Сравнение

| Параметр | Claude API | Groq API |
|----------|-----------|----------|
| Стоимость | $3-15 за 1M токенов | **БЕСПЛАТНО** |
| Скорость | ~5 сек | **~0.5 сек** |
| Лимиты | Нужны кредиты | 30 req/min бесплатно |
| Качество | Отлично | Очень хорошо |

---

## ❓ Проблемы?

### Ошибка: "No module named 'groq'"

```bash
pip install groq
```

### Ошибка: "GROQ_API_KEY не установлен"

Проверьте .env:
```bash
cat .env | grep GROQ
```

Должно быть: `GROQ_API_KEY=gsk_...`

### Ошибка: "Rate limit exceeded"

Groq дает 30 запросов в минуту. Подождите минуту или создайте новый ключ.

### Медленные ответы

Groq обычно очень быстрый. Проверьте интернет соединение.

---

## 💡 Лайфхаки

### Увеличить лимиты

На https://console.groq.com/ можете добавить платежную карту и получить:
- **14,400** запросов в день (бесплатно)
- Приоритетный доступ

### Использовать разные модели

В коде можно изменить модель:

```python
model="llama-3.3-70b-versatile"  # Быстрая и универсальная (по умолчанию)
model="mixtral-8x7b-32768"       # Большой контекст (32k токенов)
model="llama-3.1-8b-instant"     # Супер быстрая, но проще
```

### Кэширование ответов

Можно добавить кэш для популярных вопросов, чтобы не тратить лимиты.

---

**Готово! Теперь у вас полностью бесплатный и быстрый бот!** 🎉
