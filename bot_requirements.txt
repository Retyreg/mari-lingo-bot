# Зависимости для Mari Lingo Bot

# Telegram Bot
python-telegram-bot>=20.0

# LLM через OpenRouter (OpenAI-совместимый клиент)
openai>=1.30.0

# RAG компоненты.
# Раньше были закомментированы со ссылкой на requirements.txt, которого в этом
# репозитории нет, — установка строго по README не поднимала бота вообще.
# Тянут за собой torch (~2 ГБ). Без них бот стартует в degraded-режиме:
# отвечает, но без контекста из базы знаний.
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Дополнительные утилиты
python-dotenv>=1.0.0  # Для .env файлов
aiohttp>=3.9.0        # Асинхронные HTTP запросы
